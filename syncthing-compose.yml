version: "3.4"
services:
  #vpn:
    #container_name: vpn
    #image: dperson/openvpn-client:latest
    #cap_add:
    #  - net_admin # required to modify network interfaces
    #restart: unless-stopped
    #volumes:
    #  - /dev/net:/dev/net:z # tun device
    #  - ${ROOT}/config/vpn:/vpn # OpenVPN configuration
    #security_opt:
    #  - label:disable
    #ports:
    #  - 8112:8112 # port for deluge web UI to be reachable from local network
    #command: '-f "" -r 192.168.1.0/24' # enable firewall and route local network traffic

  deluge:
    container_name: deluge
    image: linuxserver/deluge:latest
    restart: unless-stopped
   # network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - ${ROOTHDD}/downloads:/downloads # downloads folder
      - ${ROOT}/config/deluge:/config # config files
    ports:
      - target: 8112
        published: 8112
        protocol: tcp 
        mode: ${MODE}

      - target: 5846
        published: 5846
        protocol: tcp 
        mode: ${MODE}


  jackett:
    container_name: jackett
    image: linuxserver/jackett:latest
    restart: unless-stopped
   # network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${ROOTHDD}/downloads/torrent-blackhole:/downloads # place where to put .torrent files for manual download
      - ${ROOT}/config/jackett:/config # config files
    ports:
      - target: 9117
        published: 9117
        protocol: tcp 
        mode: ${MODE}


  sabnzbd:
    container_name: sabnzbd
    image: linuxserver/sabnzbd
    restart: unless-stopped
    #network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - ${ROOTHDD}/downloads:/downloads # download folder
      - ${ROOT}/config/sabnzbd:/config # config files
    ports:
      - target: 8080
        published: 6799  # ####evtl switcged
        protocol: tcp 
        mode: ${MODE}

  sonarr:
    container_name: sonarr
    image: linuxserver/sonarr:latest
    restart: unless-stopped
    #network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${ROOT}/config/sonarr:/config # config files
      - ${ROOTHDD}/complete/tv:/tv # tv shows folder
      - ${ROOTHDD}/downloads:/downloads # download folder
    ports:
      - target: 8989
        published: 8989
        protocol: tcp 
        mode: ${MODE}

  radarr:
    container_name: radarr
    image: linuxserver/radarr:latest
    restart: unless-stopped
    #network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ${ROOT}/config/radarr:/config # config files
      - ${ROOTHDD}/complete/movies:/movies # movies folder
      - ${ROOTHDD}/downloads:/downloads # download folder
    ports:
      - target: 7878
        published: 7878
        protocol: tcp 
        mode: ${MODE}


  plex-server:
    container_name: plex-server
    image: linuxserver/plex:${PLEXTAG}
    restart: unless-stopped

    environment:
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - ${ROOT}/config/plex/db:/config # plex database
      - ${ROOTHDD}/plextranscode:/transcode # temp transcoded files
      - ${ROOTHDD}/complete:/data # media library
      - ${ROOTNAS}/music:/music # music
    ports:
      - target: 32400
        published: 32400
        protocol: tcp 
        mode: ${MODE}

  bazarr:
    container_name: bazarr
    image: linuxserver/bazarr
    restart: unless-stopped
    #network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
      - UMASK_SET=022 #optional
    volumes:
      - ${ROOT}/config/bazarr:/config # config files
      - ${ROOTHDD}/complete/movies:/movies # movies folder
      - ${ROOTHDD}/complete/tv:/tv # tv shows folder
    ports:
      - target: 6767
        published: 6767
        protocol: tcp 
        mode: ${MODE}

      
  prowlarr:
    container_name: prowlarr
    image: linuxserver/prowlarr:nightly
    restart: unless-stopped
    #network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - ${ROOT}/config/prowlarr:/config # config files
    ports:
      - target: 9696
        published: 9696
        protocol: tcp 
        mode: ${MODE}

      
  lidarr:
    container_name: lidarr
    image: linuxserver/lidarr:nightly
    restart: unless-stopped
    #network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - ${ROOT}/config/lidarr:/config # config files
      - ${ROOTHDD}/complete/music:/music # music folder
      - ${ROOTHDD}/downloads:/downloads # download shows folder
    ports:
      - target: 8686
        published: 8686
        protocol: tcp 
        mode: ${MODE}

      
  readarr:
    container_name: readarr
    image: linuxserver/readarr:nightly
    restart: unless-stopped
    #network_mode: ${NETWORKMODE}
    environment:
      - PUID=${PUID} # default user id, defined in .env
      - PGID=${PGID} # default group id, defined in .env
      - TZ=${TZ} # timezone, defined in .env
    volumes:
      - ${ROOT}/config/readarr:/config # config files
      - ${ROOTHDD}/complete/books:/books # music folder
      - ${ROOTHDD}/downloads:/downloads # download shows folder
    ports:
      - target: 8787
        published: 8787
        protocol: tcp 
        mode: ${MODE}


